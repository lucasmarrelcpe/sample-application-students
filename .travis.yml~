language: java
script: 
  - mvn clean verify
  - echo "$PASSWORD" | docker login -u "$LOGIN" --password-stdin
  - docker build -t changelog sample-application-db-changelog-job/
  - docker tag changelog changelog
  - docker push changelog
cache:
  directories:
    - $HOME/.m2
services:
  - docker
