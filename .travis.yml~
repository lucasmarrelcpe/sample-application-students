language: java
script: 
  - mvn clean verify
  - echo "$login" | docker login -u "$password" --password-stdin
  - docker build -t travis-ci-build-stages-demo .
cache:
  directories:
    - $HOME/.m2
services:
  - docker
